# 跨设备验收矩阵

自动测试不能替代真实 Android 生命周期、权限弹窗和无线网络切换。本表用于发布前人工验收；每次正式发布都应保存设备型号、系统版本、应用版本、网络环境和日志包。

## 当前证据

| 项目 | 状态 | 证据 |
| --- | --- | --- |
| Windows Debug 自动测试 | 通过 | 2026-09-22，CTest 7/7 |
| Windows Release 自动测试 | 通过 | 2026-09-09，CTest 7/7 |
| Windows 自包含运行目录 | 通过 | 无 Qt/MinGW `PATH` 启动与 DLL 恢复测试 |
| Android arm64 Debug APK | 通过 | API 36 构建成功 |
| Windows ↔ Android 真机 | 待验证 | 当前 ADB 无设备 |
| Android 摄像头/录音/屏幕共享 | 待验证 | 需要真实权限和 MediaProjection 流程 |
| Android 后台与网络切换 | 待验证 | 需要锁屏、切网和系统回收场景 |

## 必测环境

| 编号 | 发送端 | 接收端 | 网络 | 结果 |
| --- | --- | --- | --- | --- |
| E1 | Windows 11 | Windows 11 | 同一路由器 | 未执行 |
| E2 | Windows 11 | Android 13+ | 同一 Wi-Fi | 未执行 |
| E3 | Android 13+ | Windows 11 | 同一 Wi-Fi | 未执行 |
| E4 | Android 13+ | Android 13+ | 同一 Wi-Fi | 未执行 |
| E5 | Windows 11 | Android 13+ | Windows 移动热点 | 未执行 |

## 功能与生命周期

| 编号 | 场景 | 通过条件 | 结果 |
| --- | --- | --- | --- |
| F1 | 首次配对并拒绝 | 双方回到未配对，不开放发送和媒体入口 | 未执行 |
| F2 | 首次配对并接受 | 双方显示在线，消息和文件可双向发送 | 未执行 |
| F3 | 重启双方应用 | 发现后自动认证重连，不再次弹首次配对 | 未执行 |
| F4 | 解除配对 | 活动连接和未完成任务取消，历史消息保留 | 未执行 |
| F5 | 未配对查看历史 | 可阅读旧消息，但发送入口禁用 | 未执行 |
| F6 | 1 GiB 文件正常传输 | 最终大小和 SHA-256 与源文件一致 | 未执行 |
| F7 | 传输中断后恢复 | 从非零偏移继续，不重新发送完整文件 | 未执行 |
| F8 | 发送源文件被修改 | 任务失败并显示明确错误，不生成错误完成状态 | 未执行 |
| F9 | 同名文件 | 不覆盖已有文件，生成可识别的新文件名 | 未执行 |
| F10 | 自定义 Android 接收目录 | 重启后仍显示并写入所选目录 | 未执行 |
| F11 | 从 Android 文件选择器返回 | 配对长连接可恢复，选择的文件可发送 | 未执行 |
| F12 | Android 锁屏 10 分钟 | 前台服务存在，发现/传输按产品设定继续 | 未执行 |
| F13 | Wi-Fi 切换 | 旧连接释放，新网络发现并重连，无永久假在线 | 未执行 |
| F14 | 摄像头通话 | 权限允许后双方画面更新，挂断后画面清空 | 未执行 |
| F15 | 拒绝媒体权限 | 应用不崩溃，提示可理解，可跳转设置 | 未执行 |
| F16 | Android 屏幕共享 | MediaProjection 授权后持续更新，退出后停止并清空 | 未执行 |
| F17 | 通话中切后台再恢复 | 不崩溃；状态、通知和采集符合平台限制 | 未执行 |
| F18 | 对方强制结束进程 | 本端在超时后回到空闲并清除最后画面 | 未执行 |

## 性能记录

测试前关闭云同步和下载任务，记录 Wi-Fi 频段与链路速率。

| 指标 | 测试方法 | 结果 |
| --- | --- | --- |
| 1 GiB 平均吞吐 | 完成时间与有效文件字节数计算 | 待测 |
| 断点恢复偏移 | 日志中的 resume offset | 待测 |
| SHA-256 校验耗时 | 协商完成至 completed 的校验阶段 | 待测 |
| 峰值内存 | Windows 任务管理器 / Android Studio Profiler | 待测 |
| 视频平均帧率 | 诊断日志帧计数，连续 3 分钟 | 待测 |
| 视频平均带宽 | 系统网络统计，连续 3 分钟 | 待测 |

## 执行记录模板

```text
日期：
版本/提交：
设备与系统：
网络环境：
执行编号：
结果：通过 / 失败
现象：
诊断包路径：
关联问题：
```

